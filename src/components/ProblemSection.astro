<section class="problem section">
  <div class="container">
    <div class="problem-content">
      <h2 class="section-title">
        Tired of Being <span class="text-accent">Stuck</span> and <span class="text-accent">Undervalued</span>?
      </h2>
      <div class="problems-grid">
        <div class="problem-card card fade-in">
          <div class="problem-icon">🏭</div>
          <h3>Physically Demanding Jobs</h3>
          <p>Working in factories, retail, or labor-intensive roles that leave you exhausted with little room for growth.</p>
        </div>
        <div class="problem-card card fade-in">
          <div class="problem-icon">💸</div>
          <h3>Low-Paying Freelance Gigs</h3>
          <p>Stuck chasing $50 projects on Upwork while competing with hundreds of other writers for scraps.</p>
        </div>
        <div class="problem-card card fade-in">
          <div class="problem-icon">📉</div>
          <h3>No Career Growth</h3>
          <p>Feeling trapped in dead-end jobs with no clear path to better opportunities or financial stability.</p>
        </div>
        <div class="problem-card card fade-in">
          <div class="problem-icon">🚫</div>
          <h3>Education Barriers</h3>
          <p>Believing you need expensive degrees or technical training to break into well-paying careers.</p>
        </div>
        <div class="problem-card card fade-in">
          <div class="problem-icon">🌍</div>
          <h3>Starting Over Challenges</h3>
          <p>Whether you're new to the country or changing careers, feeling uncertain about how to build a stable future.</p>
        </div>
        <div class="problem-card card fade-in">
          <div class="problem-icon">⏰</div>
          <h3>Time and Energy Drain</h3>
          <p>Working multiple jobs or long hours just to make ends meet, with no time to invest in your future.</p>
        </div>
      </div>
      <div class="solution-preview">
        <p class="solution-text">
          <strong>What if there was a proven path to six figures?</strong> What if you could use your natural communication skills to land a stable, remote tech career without a degree, without coding, and without starting from scratch?
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .problem {
    background: var(--bg-secondary);
  }

  .section-title {
    font-size: 2.5rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 3rem;
    color: var(--text-primary);
  }

  .problems-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .problem-card {
    text-align: center;
    transition: transform 0.3s ease;
  }

  .problem-card:hover {
    transform: translateY(-8px);
  }

  .problem-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .problem-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .problem-card p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .solution-preview {
    background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
    padding: 2rem;
    border-radius: 1rem;
    text-align: center;
    margin-top: 2rem;
  }

  .solution-text {
    color: white;
    font-size: 1.25rem;
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .section-title {
      font-size: 2rem;
    }

    .problems-grid {
      grid-template-columns: 1fr;
    }

    .solution-text {
      font-size: 1.125rem;
    }
  }
</style>