<section class="hero section-lg">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text fade-in-left">
        <h1 class="hero-title">
          Break Into Tech and Earn <span class="text-gradient">Six Figures</span><br>
          as a Technical Writer<br>
          <span class="text-accent pulse">No Degree Required</span>
        </h1>
        <p class="hero-subtitle fade-in stagger-1">
          Transform your life with a stable, six-figure remote technical writing career in tech. Whether you're a factory worker, freelancer, recent immigrant, or career changer. This program is designed for ambitious people ready to upgrade their lives without going back to school.
        </p>
        <div class="hero-stats fade-in stagger-2">
          <div class="stat bounce-in stagger-1">
            <span class="stat-number text-success">50+</span>
            <span class="stat-label">Lives Transformed</span>
          </div>
          <div class="stat bounce-in stagger-2">
            <span class="stat-number text-success">$120K</span>
            <span class="stat-label">Avg. Annual Salary</span>
          </div>
          <div class="stat bounce-in stagger-3">
            <span class="stat-number text-success">90%</span>
            <span class="stat-label">Success Rate</span>
          </div>
        </div>
        <div class="hero-cta fade-in stagger-3">
          <a href="#enroll" class="btn btn-primary btn-lg">Start Your Transformation</a>
          <a href="#program" class="btn btn-secondary">Learn More</a>
        </div>
      </div>
      <div class="hero-image fade-in-right">
        <div class="image-container float">
          <img 
            src="https://images.pexels.com/photos/7688460/pexels-photo-7688460.jpeg?auto=compress&cs=tinysrgb&w=800" 
            alt="Professional technical writer working remotely"
            class="hero-img"
          />
          <div class="image-overlay"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: var(--gradient-bg);
    padding-top: 6rem;
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(249, 115, 22, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: var(--text-primary);
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-stats {
    display: flex;
    gap: 2rem;
    margin-bottom: 2.5rem;
  }

  .stat {
    text-align: center;
    padding: 1rem;
    background: var(--card-bg);
    border-radius: 0.75rem;
    box-shadow: 0 4px 6px var(--card-shadow);
    transition: transform 0.3s ease;
  }

  .stat:hover {
    transform: translateY(-4px);
  }

  .stat-number {
    display: block;
    font-size: 2rem;
    font-weight: 700;
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
  }

  .btn-lg {
    padding: 1rem 2.5rem;
    font-size: 1.125rem;
  }

  .hero-image {
    position: relative;
  }

  .image-container {
    position: relative;
    border-radius: 1rem;
    overflow: hidden;
    box-shadow: 0 20px 40px var(--card-shadow-hover);
  }

  .hero-img {
    width: 100%;
    height: auto;
    display: block;
    transition: transform 0.3s ease;
  }

  .image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, rgba(30, 58, 138, 0.1) 0%, rgba(249, 115, 22, 0.1) 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .image-container:hover .hero-img {
    transform: scale(1.05);
  }

  .image-container:hover .image-overlay {
    opacity: 1;
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 4rem;
      min-height: auto;
    }

    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
    }

    .hero-stats {
      justify-content: center;
      flex-wrap: wrap;
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
    }
  }
</style>